# Agent Rules

## Git Workflow
The user has explicitly requested that ALL future code changes should be committed and pushed to the origin repository: `https://github.com/JRiteeshReddy/GitamSIPquiz.git`.
Whenever you make a modification, complete your turn by running `git add .`, `git commit -m "<descriptive message>"`, and `git push`.
